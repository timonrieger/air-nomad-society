[project]
name = "air-nomad-society"
description = "Personalized flight deals delivered to your inbox."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.14"
authors = [{name = "Timon Rieger", email = "contact@timonrieger.de"}]
version = "0.1.0"

dependencies = [
    "database-service>=1.5.3,<2.0.0",
    "email-validator>=2.1.1,<3.0.0",
    "jinja2>=3.1.6,<4.0.0",
    "psycopg2-binary>=2.9.9,<3.0.0",
    "pydantic>=2.13.4,<3.0.0",
    "pydantic-settings>=2.15.0,<3.0.0",
    "python-dotenv>=1.0.0,<2.0.0",
    "requests>=2.32.0,<3.0.0",
]

[project.urls]
Repository = "https://github.com/timonrieger/air-nomad-society"

[dependency-groups]
dev = [
    "pytest>=9.0.0,<10.0.0",
    "pytest-cov>=7.0.0,<8.0.0",
    "ty>=0.0.55,<0.0.56",
]

[tool.uv]
# `src` is the import root (from src.services import ...); the project is run
# via entrypoints, not installed as a distribution.
package = false
add-bounds = "major"

[tool.uv.sources]
database-service = { git = "https://github.com/timonrieger/database-service.git" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
source = ["src"]
