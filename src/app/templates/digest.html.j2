<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="color-scheme" content="light">
  <title>Your weekly flight deals</title>
  <!--[if !mso]><!-->
  <link href="https://fonts.googleapis.com/css2?family=Bitter:wght@400;600;700&display=swap" rel="stylesheet" type="text/css">
  <!--<![endif]-->
</head>

{% macro button(href, label) -%}
<a href="{{ href }}" style="display: inline-block; background-color: {{ t.accent }}; color: #ffffff; text-decoration: none; font-family: {{ t.font }}; font-weight: 600; font-size: 16px; line-height: 24px; padding: 10px 28px; border-radius: 8px;">{{ label }}</a>
{%- endmacro %}

{% macro deal_card(flight) -%}
<table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="margin-bottom: 24px;">
  <tr>
    <td style="background-color: {{ t.gray_100 }}; border-radius: 12px;">
      <img src="{{ flight.image_url }}" alt="{{ flight.arr_city }}, {{ flight.arr_country }}" width="600" style="display: block; width: 100%; height: 240px; object-fit: cover; border: 0; border-radius: 12px 12px 0 0;">
      <table role="presentation" width="100%" cellpadding="0" cellspacing="0">
        <tr>
          <td style="padding: 20px 24px 24px;">
            <p style="margin: 0 0 4px; font-family: {{ t.font }}; font-size: 20px; font-weight: 700; color: {{ t.gray_900 }};">{{ flight.dep_city }} &ndash; {{ flight.arr_city }}</p>
            <p style="margin: 0 0 16px; font-family: {{ t.font }}; font-size: 14px; color: {{ t.gray_500 }};">{{ flight.arr_country }} &middot; {{ flight.from_dt }} - {{ flight.to_dt }}</p>
            <table role="presentation" width="100%" cellpadding="0" cellspacing="0">
              <tr>
                <td style="vertical-align: middle;">
                  <strong style="font-family: {{ t.font }}; font-size: 22px; font-weight: 700; color: {{ t.accent }};">{{ flight.price }} {{ flight.currency }}</strong>
                </td>
                <td align="right" style="vertical-align: middle;">
                  {{ button(flight.link, "Book Now") }}
                </td>
              </tr>
            </table>
          </td>
        </tr>
      </table>
    </td>
  </tr>
</table>
{%- endmacro %}

{% macro empty_state(message) -%}
<table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="margin-bottom: 24px;">
  <tr>
    <td align="center" style="background-color: {{ t.gray_100 }}; border-radius: 12px; padding: 32px 28px;">
      <p style="margin: 0 0 8px; font-family: {{ t.font }}; font-size: 18px; font-weight: 700; color: {{ t.gray_900 }};">No flights found</p>
      <p style="margin: 0 0 20px; font-family: {{ t.font }}; font-size: 15px; line-height: 1.5; color: {{ t.gray_500 }};">{{ message }}</p>
      {{ button(base_url ~ "/subscribe?token=" ~ update_token, "Update Profile") }}
    </td>
  </tr>
</table>
{%- endmacro %}

{% macro section_heading(text) -%}
<h2 style="margin: 0 0 16px; font-family: {{ t.font }}; font-size: 22px; font-weight: 700; color: {{ t.gray_900 }};">{{ text }}</h2>
{%- endmacro %}

<body style="margin: 0; padding: 0; background-color: #ffffff; -webkit-text-size-adjust: none; text-size-adjust: none;">
  <div style="display: none; max-height: 0; overflow: hidden;">Your weekly flight deals are here.</div>
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background-color: #ffffff;">
    <tr>
      <td align="center" style="padding: 0 16px;">
        <table role="presentation" width="600" cellpadding="0" cellspacing="0" style="max-width: 600px; text-align: left;">
          <tr>
            <td style="padding: 48px 0 40px;">
              <p style="margin: 0 0 24px; font-family: {{ t.font }}; font-size: 14px; font-weight: 600; letter-spacing: 1px; text-transform: uppercase; color: {{ t.accent }};">Air Nomad Society</p>
              <h1 style="margin: 0; font-family: {{ t.font }}; font-size: 32px; font-weight: 700; line-height: 1.2; color: {{ t.gray_900 }};">Hi {{ username }}!</h1>
              <p style="margin: 12px 0 0; font-family: {{ t.font }}; font-size: 16px; line-height: 1.5; color: {{ t.gray_500 }};">Here are this week's flight deals, picked for you.</p>
            </td>
          </tr>
          <tr>
            <td>
              {{ section_heading("Visit favorite countries...") }}
              {% if dream_flights %}
                {% for flight in dream_flights %}{{ deal_card(flight) }}{% endfor %}
              {% else %}
                {{ empty_state("Unfortunately, we couldn't find any flight deals for your favorite countries. This could be due to either a lack of countries favorited by you or unavailable flights to these countries. To add more countries, update your profile using the button below.") }}
              {% endif %}
            </td>
          </tr>
          <tr>
            <td style="padding-top: 16px;">
              {{ section_heading("...and find secret gems") }}
              {% if gem_flights %}
                {% for flight in gem_flights %}{{ deal_card(flight) }}{% endfor %}
              {% else %}
                {{ empty_state("Unfortunately, we couldn't find any flight deals for your secret gems. This may be due to limited flight availability to these countries or if you've favorited a large number of countries, leaving none remaining. You can update your profile using the button below.") }}
              {% endif %}
            </td>
          </tr>
          <tr>
            <td align="center" style="padding: 24px 0 8px;">
              <p style="margin: 0; font-family: {{ t.font }}; font-size: 20px; font-weight: 700; color: {{ t.gray_900 }};">Happy travels!</p>
            </td>
          </tr>
          <tr>
            <td align="center" style="border-top: 1px solid {{ t.gray_200 }}; padding: 24px 0 48px;">
              <p style="margin: 0; font-family: {{ t.font }}; font-size: 12px; color: {{ t.gray_500 }};">
                <a href="{{ base_url }}" style="color: {{ t.gray_500 }}; text-decoration: none; padding: 0 10px;">timonrieger.de</a>
                <a href="{{ base_url }}/unsubscribe?token={{ unsubscribe_token }}" style="color: {{ t.gray_500 }}; text-decoration: none; padding: 0 10px;">Unsubscribe</a>
                <a href="{{ base_url }}/subscribe?token={{ update_token }}" style="color: {{ t.gray_500 }}; text-decoration: none; padding: 0 10px;">Update Profile</a>
              </p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
